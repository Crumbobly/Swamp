package ru.app.web.models

import org.http4k.template.ViewModel
import java.time.LocalDateTime
import java.time.format.TextStyle
import java.util.*

class WelcomePageVM(): ViewModel {

//    val currentYear: Int = currentDateTime.year
//    val currentMount: String = currentDateTime.month.getDisplayName(TextStyle.FULL, Locale.forLanguageTag("ru"))
//        .replaceFirstChar { if (it.isLowerCase()) it.titlecase(Locale.getDefault()) else it.toString() }
//    val currentDay: Int = currentDateTime.dayOfMonth
//    val currentDayName: String = currentDateTime.dayOfWeek.getDisplayName(TextStyle.FULL, Locale.forLanguageTag("ru"))
//        .replaceFirstChar { if (it.isLowerCase()) it.titlecase(Locale.getDefault()) else it.toString() }

}